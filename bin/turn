#!/usr/bin/env ruby
require_relative '../lib/turn'

board = Array.new
for i in 0..8
  board[i] = " "
end

puts "Welcome to Tic Tac Toe!"

move(board, 4, "X")
move(board, 0, "O")
move(board, 1, "X")

display_board(board)

turn(board)